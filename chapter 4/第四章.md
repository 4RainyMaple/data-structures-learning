## 4.1

树的递归定义：  
树是节点的集合。若树不是空集，则由根的节点r和多个子树组成。  
每一棵子树的根记作根r的儿子，r是每一棵子树的父亲，没有儿子的节点称为树叶。  
一棵树是n个节点和n-1边的集合。  
从节点$`n_1`$到$`n_k`$的路径定义为$`n_1`$到$`n_k`$的一个序列，则$`n_i`$是$`n_{i+1}`$的父亲。  
路径的长定义为路径上边的个数。  
节点的深度定义为根到该节点的长（唯一）。  
节点的高定义为该节点到树叶的最长路径的长，一棵树的高等于根的高。

树的实现：

![树的第一个儿子与下一个兄弟表示法](./images/tree-first-child-next-sibling.png)

每个节点只记录数据，第一个儿子，以及下一个兄弟。

## 4.2

二叉树：每个节点儿子数至多2的树。  
平均深度 $O(\sqrt{N})$。

一个例子——表达式树：

![二叉表达式树](./images/binary-expression-tree.png)

## 4.3

二叉查找树（这里假设节点元素均为整数）

性质：对于每个节点，其储存元素x，则其左节点的元素均小于x，其右节点的元素均大于x。二叉搜索树平均深度 $O(\log N)$。

删除节点的机制：

![二叉查找树删除有两个孩子的节点](./images/bst-delete-two-children.png)

![二叉查找树删除至多一个孩子的节点](./images/bst-delete-at-most-one-child.png)

## 4.4

### AVL树

定义：**每个节点**的左右子树深度差最多为1的二叉查找树。其中空树的高度定义为-1。

因此，给定树的高度 $h$，最少节点数 $S(h) = S(h-1)+S(h-2)+1$，其中1表示根节点，另外两项表示两棵子树的节点数，容易看出这是斐波那契数列的变形。

由于树的高度为 $O(\log N)$，因此除了插入删除等操作以外的操作时间复杂度基本都为 $O(\log N)$。而插入删除操作可能破坏AVL树的平衡结构，因此需要修正树——旋转。

以下实例源自b站@蓝不过海呀。

### 单旋转

对于节点 $a$，不平衡出现在对 $a$ 的左儿子的左子树进行插入或对 $a$ 的右儿子的右子树进行插入，如图所示：

![AVL树单旋转示意图](./images/avl-single-rotation.png)

#### 实例1：LL型

![LL型旋转前](./images/avl-ll-before.png)

LL型：失衡节点右旋（顺时针），冲突的右儿子变为左儿子。

![LL型右旋后](./images/avl-ll-after-right-rotation.png)

#### 实例2：RR型

![RR型旋转前](./images/avl-rr-before.png)

RR型：失衡节点左旋（逆时针），冲突左儿子变右儿子。

![RR型左旋后](./images/avl-rr-after-left-rotation.png)

### 双旋转

有些情况单旋转不足以维持树的平衡，如图所示：

![需要双旋转的失衡情况](./images/avl-double-rotation-needed.png)

此时就需要引入双旋转：

![AVL树双旋转示意图](./images/avl-double-rotation.png)

此处B和C画作比D深1.5格表示不确定哪个深两格，但这不重要。

#### 实例3：LR型

![LR型旋转前](./images/avl-lr-before.png)

LR型：①失衡节点左儿子左旋。

![LR型第一步：左儿子左旋](./images/avl-lr-step-1-left-rotation.png)

②失衡节点右旋。

![LR型第二步：失衡节点右旋](./images/avl-lr-step-2-right-rotation.png)

#### 实例4：RL型

![RL型旋转前](./images/avl-rl-before.png)

RL型：①失衡节点右儿子右旋。

![RL型第一步：右儿子右旋](./images/avl-rl-step-1-right-rotation.png)

②失衡节点左旋。

![RL型第二步：失衡节点左旋](./images/avl-rl-step-2-left-rotation.png)

### 插入后两个节点失衡

若插入后两个节点失衡，只需旋转距离最近的节点即可，如图所示：

![插入后两个节点失衡](./images/avl-insertion-two-unbalanced-nodes.png)

此时只需将6左旋。

![节点6左旋后恢复平衡](./images/avl-insertion-rebalance-node-6.png)

补充：上述节点旋转后中序输出是不变的，因此旋转前后二叉树等效。

### 删除节点后的失衡

节点的删除导致的失衡情况处理方法与上述相同，但值得注意的是，删除节点可能会造成连环失衡，因此需要一直旋转直到平衡。

#### 实例5

如图所示，有一棵AVL树：

![删除节点5之前的AVL树](./images/avl-delete-5-before.png)

现在要删除5：

![删除节点5后出现失衡](./images/avl-delete-5-unbalanced.png)

先右旋9再左旋7：

![双旋转后的AVL树](./images/avl-delete-5-after-double-rotation.png)

此时11失衡，左旋11即可：

![再次旋转后恢复平衡](./images/avl-delete-5-balanced.png)

## 4.7 B 树

**1. 为什么需要B树：**  
数据太多，需要储存在磁盘上，磁盘访问代价高，因此希望树的分支尽可能多，如此查找次数更少。  

**2. B树的核心结构：**  
设 M 为一个内部节点最多拥有的孩子数，L 为一个叶子最多存储的数据项数。  
则B树的核心部分满足以下规则：

- 数据储存在叶子中，所有叶子深度相同。
- 内部节点存放指路的关键字（用于比较大小，方便查找的值，即路标），有k个孩子就  有k-1个关键字。  

比如：该节点有4个关键字，因此有5个孩子

![B树内部节点的4个关键字与5个孩子](./images/b-tree-internal-node-keys-and-children.png)

- 非根叶子至少 ⌈L/2⌉ 个数据项
- 非根内部节点至少 ⌈M/2⌉ 个孩子
- 非叶子的根至少2 个孩子  

**3. B树的查找规则：**  
根据关键字进入孩子，一直找到叶子才能确定数据是否存在。  

**4. B树的插入：满了就分裂**

- 未超过 L 项：直接完成。  
- 超过 L 项：分成两个叶子，父节点增加一个孩子并更新关键字。  
- 父节点也超限：继续向上分裂。  
- 根也分裂：建立新根，树高增加 1。  

**5. 删除：不够就借，借不到就合并**  
找到叶子并删除数据后：

- 大于等于最低数量：完成，必要时更新指路关键字。

更新关键字举例：

```text
 删除前：
          [20]
         /    \
 [5,10,15]  [20,25,30,35]

 删除后：
          [25]
         /    \
 [5,10,15]  [25,30,35]
```

- 低于最低数量：向有余量的相邻兄弟借。  
- 兄弟也没有余量：合并，父节点减少一个孩子。  
- 父节点孩子因此不足：继续向上调整。  
- 根只剩一个孩子：让孩子成为新根，树高减少 1。
